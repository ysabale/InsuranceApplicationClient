import { ButtonRendererComponent } from './button-renderer-component';

describe('ButtonRendererComponent', () => {
  it('should create an instance', () => {
    expect(new ButtonRendererComponent()).toBeTruthy();
  });
});
